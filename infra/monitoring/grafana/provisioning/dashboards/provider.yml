apiVersion: 1

providers:
  - name: William OS Dashboards
    orgId: 1
    folder: William OS
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
